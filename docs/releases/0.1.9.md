# Jellyfin on Demand 0.1.9 (hotfix)

## Lucky + mature warm
- Card **Lucky** → `POST /JellyfinOnDemand/swarm/lucky` → play-bind → PlayNow
- Native warm: `max(32MiB, 5%)` tail with `dont_download` elsewhere until tail complete, then head, then sequential

```bash
make up
# tag v0.1.9
```
