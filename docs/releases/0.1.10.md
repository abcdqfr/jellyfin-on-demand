# Swarmplay 0.1.10 (hotfix)

## Extent gate enforcement
Play-bind no longer fail-opens when a sparse growing file reports a large `Length`.
Ready requires native tail→head warm complete (strmarr PreparePlay shape).

```bash
make up
```
