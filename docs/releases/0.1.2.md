# Swarmplay 0.1.2 (hotfix)

## Why
Play on a TMDB card toasted “Torznab/Ensure still warming up” forever: lucky
used fixture releases without magnets, and saved Torznab URLs were path-mangled
so live indexer search could not work.

## Fix
- Server `POST /Swarmplay/swarm/lucky` (configured Torznab → #1 → play-bind)
- Magnet from Prowlarr `<guid>`; URL normalize for `http:/` / cwd-prefix footguns
- Honest error toasts (`torznab_empty`, `no_magnet`, `not_ready`, …)

## Note
O6a virtual-item Id → Desktop Play is still incomplete; when Ensure is ready we
attempt `playbackManager.play` with the growing Path. Full JF item bind is next.

```bash
make up   # 0.1.2
# tag v0.1.2
```
