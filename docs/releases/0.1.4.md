# Swarmplay 0.1.4 (hotfix)

## Picker sizes
Prowlarr exposes size as RSS `<size>`. Parser now reads element first, then
`torznab:attr name="size"`. Picker should show MB/GB instead of `0B` / `size ?`.

## Real play after select
1. Pick a release → toast “warming swarm…”
2. `play-bind` Ensure + wait ready
3. `playbackManager.play` with **Http** MediaSource → `GET /Swarmplay/swarm/stream?btih=&fileIndex=&api_key=`
4. Toast “playing” only if a player actually engages; otherwise an honest failure toast

Path-only synthetic items were a no-op in Jellyfin Desktop.

```bash
make up
# tag v0.1.4
```
