# Jellyfin on Demand 0.1.11 (hotfix)

## strmarr-shaped extent gate
- Floors: **8 MiB** head + tail (strmarr `minWarmupBytes` / `DefaultExtentHeadFloor`)
- Order: tail → head → sequential, with `set_piece_deadline`
- `swarm_ensure` **blocks** (≤180 s) until both bands `have_piece` — client must not open the player earlier
- Play picker: Batch default; empty filter fallback

```bash
make up
```
