# Swarmplay 0.1.7 (hotfix)

## No substitute player
0.1.6’s fullscreen `<video>` overlay is gone. Warm still runs; playback starts
only through Jellyfin’s `playbackManager` and real OSD. If that is unavailable
or does not engage → toast failure. Next: O6a virtual-item bind so JF Play +
transcode work like strmarr.

```bash
make up
# tag v0.1.7
```
