# Jellyfin on Demand 0.1.6 (hotfix)

## Cache on disk (btrfs), not tmpfs
`/tmp` is RAM-backed tmpfs on this host. Growing torrent files now live at
`/home/brandon/cache/jellyfin-on-demand` (sibling of `strmarr` / `arr` on the 1.7T
btrfs). Override with `JELLYFIN_ON_DEMAND_CACHE_DIR`; deploy refuses `/tmp` roots.
Systemd drop-in sets the env for Jellyfin.

## Playback
Jellyfin 10.11 does not put `playbackManager` on `window`. After warm, Jellyfin on Demand
opens a fullscreen HTML5 `<video>` on `/JellyfinOnDemand/swarm/stream` (overlay).

## Title relevance
Significant-token recall + 0.67 gate so weak Torznab junk drops out.

```bash
make up
# tag v0.1.6
```
